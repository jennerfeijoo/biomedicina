name: Apply Chemistry I Unit 4

on:
  push:
    branches:
      - content/quimica-i-semester-course

permissions:
  contents: write

jobs:
  apply-unit:
    if: ${{ !contains(github.event.head_commit.message, '[skip q1-unit-04]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout course branch
        uses: actions/checkout@v4
        with:
          ref: content/quimica-i-semester-course

      - name: Materialize and run validated upgrade
        run: |
          python scripts/materialize_quimica_i_unit_04_upgrade.py
          python scripts/upgrade_quimica_i_unit_04.py

      - name: Commit upgraded unit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/generated_units/quimica-i/unit-04.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Upgrade Chemistry I unit 4 [skip q1-unit-04]"
          git push origin HEAD:content/quimica-i-semester-course
