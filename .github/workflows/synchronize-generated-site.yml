name: Synchronize generated site

on:
  pull_request:
    paths:
      - 'data/**'
      - 'docs/**'
      - 'scripts/**'
      - 'templates/**'
      - 'tests/**'
      - '.github/workflows/synchronize-generated-site.yml'

permissions:
  contents: write

concurrency:
  group: synchronize-generated-site-${{ github.ref }}
  cancel-in-progress: false

jobs:
  synchronize:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout pull-request branch
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Build canonical Laboratorio de Bioinstrumentación corpus
        shell: bash
        run: |
          set -euo pipefail
          python scripts/close_laboratorio_bioinstrumentacion_temp.py
          python scripts/validate_academic_courses.py
          python -m unittest tests.test_laboratorio_bioinstrumentacion_canonical_course

      - name: Validate structured academic sources
        shell: bash
        run: |
          set -euo pipefail
          python scripts/validate_curriculum.py
          python scripts/validate_generated_units.py
          python scripts/audit_curriculum_completeness.py
          python scripts/validate_pilot_foundations.py
          if [[ -f data/course_migrations/bioinstrumentacion-public-canonical-v1.json ]]; then
            python scripts/preflight_bioinstrumentation_atomic_migration.py
            python scripts/validate_bioinstrumentation_public_canonical.py
          fi

      - name: Regenerate site deterministically
        shell: bash
        run: |
          set -o pipefail
          python scripts/generate_site.py --force --with-units 2>&1 | tee global-generation.txt

      - name: Validate regenerated site
        shell: bash
        run: |
          python scripts/audit_public_unit_alignment.py --strict
          python scripts/validate_links.py
          python scripts/validate_academic_courses.py
          python -m unittest tests.test_laboratorio_bioinstrumentacion_canonical_course

      - name: Commit clean canonical closure
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git show origin/main:.github/workflows/synchronize-generated-site.yml > .github/workflows/synchronize-generated-site.yml
          rm -f scripts/close_laboratorio_bioinstrumentacion_temp.py global-generation.txt
          git add -A -- \
            .github/workflows/synchronize-generated-site.yml \
            scripts/close_laboratorio_bioinstrumentacion_temp.py \
            data/courses/laboratorio-bioinstrumentacion \
            ingenieria-biomedica/laboratorio-bioinstrumentacion \
            ingenieria-biomedica/index.html \
            catalogo/index.html
          if git diff --cached --quiet; then
            echo "No canonical closure changes to commit."
            exit 1
          fi
          git commit -m "Close Laboratorio de Bioinstrumentación canonical course"
          git push origin "HEAD:${GITHUB_HEAD_REF}"
