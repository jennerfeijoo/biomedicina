name: Synchronize generated site

on:
  pull_request:
    paths:
      - 'data/**'
      - 'scripts/**'
      - 'templates/**'
      - '.github/workflows/synchronize-generated-site.yml'

permissions:
  contents: write

concurrency:
  group: publish-redeveloped-courses-${{ github.ref }}
  cancel-in-progress: false

jobs:
  synchronize:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout pull-request branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply authoritative curriculum validator
        run: python scripts/_apply_curriculum_validator_patch.py

      - name: Validate curriculum and pilot foundation
        run: |
          python -m py_compile scripts/validate_curriculum.py
          python scripts/validate_curriculum.py
          python scripts/validate_pilot_foundations.py

      - name: Regenerate site deterministically
        shell: bash
        run: |
          set -o pipefail
          python scripts/generate_site.py --force --with-units 2>&1 | tee global-generation.txt

      - name: Validate regenerated site
        shell: bash
        run: |
          python scripts/audit_public_unit_alignment.py --strict
          python scripts/validate_links.py

      - name: Upload synchronization diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: generated-site-synchronization
          path: global-generation.txt
          if-no-files-found: warn

      - name: Remove one-time validator scaffolding
        shell: bash
        run: |
          rm scripts/_apply_curriculum_validator_patch.py
          git checkout origin/main -- .github/workflows/synchronize-generated-site.yml

      - name: Commit permanent validator update
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "El validador y el sitio ya están sincronizados."
            exit 0
          fi
          git commit -m "Validate authoritative editorial states"
          git push origin "HEAD:${GITHUB_HEAD_REF}"
