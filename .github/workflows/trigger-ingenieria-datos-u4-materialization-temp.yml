name: Trigger Ingenieria Datos U4 materialization temp

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  materialize:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Materialización temporal desactivada durante carga de fragmentos"
