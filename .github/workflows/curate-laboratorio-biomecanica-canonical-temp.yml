name: Curate Laboratorio Biomecanica canonical temp

on:
  push:
    branches:
      - codex/laboratorio-biomecanica-canonical-closure
    paths:
      - scripts/curate_laboratorio_biomecanica_canonical_temp.py
      - .github/workflows/curate-laboratorio-biomecanica-canonical-temp.yml

permissions:
  contents: write

jobs:
  curate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: codex/laboratorio-biomecanica-canonical-closure
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Build canonical closure
        run: python scripts/curate_laboratorio_biomecanica_canonical_temp.py
      - name: Validate canonical corpus
        run: |
          python scripts/validate_academic_courses.py
          python -m unittest tests.test_laboratorio_biomecanica_canonical_course -v
          python scripts/validate_generated_units.py
          python scripts/audit_generic_content.py
      - name: Regenerate Laboratorio de Biomecanica
        run: |
          python scripts/generate_site.py --force --subject laboratorio-biomecanica --with-units --force-authored-units
          python scripts/sync_catalog_statuses.py
          python scripts/audit_public_unit_alignment.py --strict
          python scripts/validate_links.py
      - name: Self-clean and commit
        shell: bash
        run: |
          set -euo pipefail
          rm -f scripts/curate_laboratorio_biomecanica_canonical_temp.py
          rm -f .github/workflows/curate-laboratorio-biomecanica-canonical-temp.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Close Laboratorio de Biomecánica canonical course"
          git push origin HEAD:codex/laboratorio-biomecanica-canonical-closure
